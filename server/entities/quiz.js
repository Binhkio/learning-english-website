class Quiz {
    constructor(param) {
        this.name = param.name;
        this.creator = param.creator;
        this.lessons = param.lessons;
        this.status = param.status;
    }
}

module.exports = Quiz
